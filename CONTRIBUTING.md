# Contributing

Bug reports, compatibility work, false-positive cases, and additional distribution templates are welcome. Add tests for behavioral changes, run `bundle exec rake`, and never add a blocking rule based only on a news headline. Security policies require a documented threat, bounded matching, bypass tests, and false-positive tests.
